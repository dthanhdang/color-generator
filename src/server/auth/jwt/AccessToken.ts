export const subjectPrefix = "chromagen"

export const apiAudience = "https://chromagen.meow-meow.dev/api"

export const issuer = "https://chromagen.meow-meow.dev"

export const accessTokenCookieName = "chromagen_accessToken"

export type AccessToken = {
  userId: number
}
